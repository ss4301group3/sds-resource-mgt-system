var queue = [];

class Notification {}

function push(notification) {
    //add new notification to queue
}

function send() {
    //send queued notifications
}

module.exports = {
    Notification,
    push,
    send
};