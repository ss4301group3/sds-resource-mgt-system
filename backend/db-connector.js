const { Connection, Request } = require("tedious");

//reminder to reinsert credentials and server url in azure-sql.json
const config = require("./configs/azure-sql")

module.exports = {
    //expose functions, variables here
}